/**
 * Use-case layer — application business rules.
 *
 * Interactors live here. They orchestrate domain objects through **outbound
 * ports** (repository, clock, identity, realtime). This package may import
 * `@clean-chat/domain` only.
 *
 * Planned use cases (from docs/PRODUCT.md), not implemented yet:
 * sign up / sign in / sign out, list / ensure-general / create channel,
 * list / send / delete-own message, typing upsert / clear / list,
 * presence heartbeat / list / disconnect.
 */
export {};
